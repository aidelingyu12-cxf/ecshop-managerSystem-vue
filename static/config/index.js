/**
 * dev
 */
; (function () {
  window.SITE_CONFIG = {};

  // apiリクエストアドレス
  window.SITE_CONFIG['baseUrl'] = 'http://localhost:8080/ecshop';

  // cdn = domain + version
  // window.SITE_CONFIG['domain']  = './'; // domain
  // window.SITE_CONFIG['version'] = '';   // version(年月日时分)
  //window.SITE_CONFIG['cdnUrl'] = window.SITE_CONFIG.domain + window.SITE_CONFIG.version;
})();
